The problem is that we did not set the outbox position to relative,
and so the inner was positioned absolute to the html body,
in order for it to be absolute in the outer box, the out box has to be relative.

